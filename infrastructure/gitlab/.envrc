source_up

export NIXPKGS_ALLOW_UNFREE=1

# GitLab Terraform State
op signin --account my.1password.eu

export TERRAFORM_STATE_URL="${GITLAB_TERRAFORM_STATE_URL}"
export GITLAB_ACCESS_TOKEN="op://Private/GitLab/Personal Access Tokens/accessToken"
