# Deno specific
# DENO_DIR is typically global or managed by the environment.
# If you set a local DENO_DIR (e.g., via .envrc for direnv), add it here:
# .deno_dir/

# Dagger build cache (UTF-8 folder name: .bناء)
.bنءا/
.dagger-modules/ # For older Dagger versions

# General build outputs (if your Deno project creates them)
dist/
out/
build/

# Lock file (if you choose to commit it, then don't ignore)
# deno.lock

# Environment files - ensure sensitive .env files are not committed
.env
.env.*
# It's good practice to commit an example env file
!/.env.example

# OS generated files
.DS_Store
Thumbs.db

# IDE specific (if not handled by global .gitignore)
# .vscode/
# .idea/

# Logs
logs/
*.log
```
