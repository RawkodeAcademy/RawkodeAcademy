FLAKE_ROOT=$(git rev-parse --show-toplevel)/flakes

if has nix &>/dev/null; then
  if nix flake show &>/dev/null; then
    use flake $FLAKE_ROOT/rust
    use flake .
  fi
fi

export SUPABASE_URL="http://localhost:54321"
