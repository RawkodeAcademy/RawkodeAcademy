type Episode @key(fields: "showId, code") {
  showId: ID!
	code: String!
	title: String!
	subtitle: String!
	description: String!
}

type Query {
  episodes: [Episode]
	episodeByShow(showId: String!): Episode
  episodeByCode(code: String!): Episode
}
