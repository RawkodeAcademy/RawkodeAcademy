import { describe, it, expect } from "vitest";

describe("Casting Credits Resolvers", () => {
	it("should have the correct resolver for CastingCredit.person", async () => {
		expect(true).toBe(true);
	});
});
