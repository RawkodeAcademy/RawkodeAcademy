type: application
language: typescript
platform: cloudflare

tasks:
  dev:
    command: npm run dev
    local: true
  deploy:
    command: npm run deploy
  publish-schema:
    command: npm run publish-schema
  generate-migrations:
    command: npm run generate-migrations
  migrate:
    command: npm run migrate