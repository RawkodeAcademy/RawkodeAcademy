name: Rawkode Academy Website

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/rawkode-academy-website.yml"
      - "projects/rawkode.academy/website/**"
  pull_request:
    paths:
      - ".github/workflows/rawkode-academy-website.yml"
      - "projects/rawkode.academy/website/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event_name == 'push' && 'rawkode.academy' || steps.preview.outputs.url }}
      url: ${{ github.event_name == 'push' && 'https://rawkode.academy' || steps.preview.outputs.url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Dagger
        run: |
          curl -fsSL https://dl.dagger.io/dagger/install.sh | BIN_DIR=/usr/local/bin sh

      - name: Build Website
        working-directory: projects/rawkode.academy/website
        run: |
          dagger <<.
            . | build | export dist
          .

      - name: Deploy Production
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        working-directory: projects/rawkode.academy/website
        env:
          CLOUDFLARE_WORKERS_TOKEN: ${{ secrets.CLOUDFLARE_WORKERS_TOKEN }}
        run: |
          dagger <<.
            ../../../dagger/cloudflare | deploy \
              ./dist \
              wrangler.jsonc \
              env:CLOUDFLARE_WORKERS_TOKEN
          .

      - name: Deploy Preview
        id: preview
        if: github.event_name == 'pull_request'
        working-directory: projects/rawkode.academy/website
        env:
          CLOUDFLARE_WORKERS_TOKEN: ${{ secrets.CLOUDFLARE_WORKERS_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # GitHub Actions doesn't have a direct equivalent to GitLab's CI_MERGE_REQUEST_ID
          # We'll use the PR number instead
          PR_NUMBER=${{ github.event.pull_request.number }}

          dagger <<.
            ../../../dagger/cloudflare | preview \
              ./dist \
              wrangler.jsonc \
              env:CLOUDFLARE_WORKERS_TOKEN \
              env:GITHUB_TOKEN \
              $PR_NUMBER
          .
